import React, {useEffect, useState} from 'react';

const Parent = (props) => {
    return (
        <>
            <h1 className="camel"> {props.name} </h1>
        </>
    )
}

export default Parent;